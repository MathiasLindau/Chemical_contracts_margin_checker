# Pinned from uv.lock (Python >=3.11). Used by Docker and `pip install -r requirements.txt`.
# Jupyter is optional for notebooks: pip install jupyter==1.1.1
# Docker installs CPU torch first, then this file.

minsearch==0.2.0
openai==3.3.0
openpyxl==3.1.5
pandas==3.0.5
psycopg[binary]==3.3.5
python-dotenv==1.2.3
requests==2.34.2
scikit-learn==1.9.0
sentence-transformers==6.0.1
streamlit==1.62.0
tqdm==4.70.0
